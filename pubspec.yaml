name: opd_creative_studio
description: A mobile reporting app for doctors using digital marketing services
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.64.0
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  intl: ^0.18.1
  shared_preferences: ^2.2.2
  shared_preferences_web: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true